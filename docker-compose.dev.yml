version: "3"

services:
  mariadb:
    ports:
      - 8303:3306
  php:
    environment:
      PHP_XDEBUG_ENABLED: 1
      MYSQL_PORT: 8303:3306
  nginx:
    environment:
      NGINX_ERROR_LOG_LEVEL: debug
    ports:
      - 8888:80
